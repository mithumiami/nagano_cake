<div>
  <h2>本当に退会しますか？</h2>

    <div>
     <p>退会すると、会員登録情報や</p>
     <p>これまでの購入履歴が閲覧できなくなります。</p>
     <p>退会する場合は、「退会する」をクリックしてください。</p>
    </div>

    <div>
     <%= link_to "退会しない", customer_path(current_customer.id), class: 'btn btn-primary'%>
     <%= link_to "退会する",  goodbye_path(current_customer.id), method: :patch, class: 'btn btn-danger'%>
    </div>
</div>